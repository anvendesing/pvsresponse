{
  "name": "prakruthivanam-manufacturing",
  "version": "1.0.0",
  "private": true,
  "description": "Prakruthivanam Manufacturing — Capacitor wrapper around the /mfg/ PWA (shop-floor MO + machine runs).",
  "scripts": {
    "sync": "cap sync android",
    "build:web": "cd ../erp-portal && npm run build:mfg",
    "build:android": "npm run build:web && cap sync android && cd android && gradlew.bat assembleDebug",
    "build:android:only": "cap sync android && cd android && gradlew.bat assembleDebug",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor-mlkit/barcode-scanning": "^6.2.0",
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  }
}
