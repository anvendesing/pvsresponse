# Mobile / Capacitor device build
# Copy this to .env.mobile and set the LAN IP or production URL of your backend.
# Then run: npm run build:mobile

# Replace with the IP address of the machine running the backend
# so Android devices on the same Wi-Fi network can reach the API.
VITE_API_URL=http://192.168.1.X:4000
